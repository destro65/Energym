package org.example.project

import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier

@Composable
actual fun VideoPlayer(modifier: Modifier, url: String) {
    // El reproductor de video ha sido desactivado temporalmente para restaurar la estabilidad.
    // Esta implementación vacía satisface la declaración `expect` del código común.
}
